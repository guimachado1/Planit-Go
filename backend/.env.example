NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://planit:planit@localhost:5433/planit_go
# Em produção (RDS): use DATABASE_SSL=true ou deixe "auto" (SSL automático com NODE_ENV=production)
DATABASE_SSL=auto
JWT_SECRET=change-me-to-a-long-random-string-in-production
JWT_EXPIRES_IN=7d
# Em produção: URL exata do frontend (Amplify), sem barra no final
# CORS_ORIGIN=https://main.xxxxx.amplifyapp.com
